#ifndef _MATH_MACROS_WH_H
#define _MATH_MACROS_WH_H

// Various math macros and inline functions
// By Wai Ho Li

#ifndef MAX
	#define MAX(a,b) ( (a)>(b) ? (a) : (b) )
#endif

#ifndef MIN
	#define MIN(a,b) ( (a)<(b) ? (a) : (b) )
#endif

#endif